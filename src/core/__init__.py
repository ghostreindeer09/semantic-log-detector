"""
Core contracts and shared types for the SOC Detection Engine.

This package defines the data contracts (schemas), routing logic,
and alert construction that unify the structured IDS and semantic
log analysis pipelines into a single coherent system.
"""
